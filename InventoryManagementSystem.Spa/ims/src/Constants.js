const Constants = {
        ApiUrl: `http://${window.location.hostname}:5000/api/v1`,
        Endpoints: {
            Item: 'Items',
            Service: 'Services',
            Supplier: 'Suppliers',
            Client: 'Clients',
            Auth: 'Authentication'
        }
}
export default Constants;